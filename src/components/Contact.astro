

<div id="contact"></div>
<form
    action="https://formspree.io/f/xkgjravr"
    method="POST"
    >
    <h2>Contact Me</h2>
    <label>
        Your email:
        <input type="email" name="email" required pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$" 
        title="Please enter a valid email address" placeholder="Email">
    </label>
    <label>
        Your message:
        <textarea name="message" required placeholder="Let me know what you think"></textarea>
    </label>
    <!-- your other form fields go here -->
    <button type="submit">Send</button>
</form>


<style>
    #about-me {
        background-color: rgb(7, 7, 20);
        padding: 2rem;
        margin: 0;
    }
    form {
        width: 70dvw;
		margin: 0 auto 10rem auto;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 1rem;
        margin-bottom: 3rem;
    }

    h2 {
        color: rgba(255, 251, 25, 0.658);
        font-size: 2rem;
    }

    label {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        background-color: rgba(15, 15, 44, 0.8);
        border-radius: 0.5rem;
        padding: 1rem;
        color: #d4d4d4;
        font-size: 1rem;
        transition: transform 0.3s ease, background-color 0.3s ease;
        width: 100%;
        max-width: 500px;
    }

    label:hover {
        background-color: rgba(25, 25, 64, 0.9);
        transform: scale(1.02);
    }

    input,
    textarea {
        width: 100%;
        height: 2rem;
        margin-top: 0.5rem;
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 0.5rem;
        background-color: rgba(7, 7, 20, 0.9);
        color: #d4d4d4;
        font-size: 1rem;
        outline: none;
        transition: border-color 0.3s ease, background-color 0.3s ease;
    }

    input:focus,
    textarea:focus {
        border-color: rgba(255, 251, 25, 0.658);
        background-color: rgba(15, 15, 44, 1);
    }

    button {
        background-color: rgba(255, 251, 25, 0.658);
        color: #000;
        font-size: 1rem;
        font-weight: bold;
        padding: 0.8rem 1.5rem;
        border: none;
        border-radius: 0.5rem;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.3s ease;
    }

    button:hover {
        background-color: rgba(255, 251, 25, 0.8);
        transform: scale(1.05);
    }

    button:active {
        transform: scale(0.95);
    }

</style>